Galeria digital pixi 


-------BASE DE DATOS------
-crear base de datos con las siguientes caracteristicas:
 -nombre de tabla: imagen
campos: 
id: int,
imagen: varchar(20),
id_evento: int.
Importante NO USAR LLAVE PRIMARIA.


--------NOTAS--------
existen 2 conexiones a la base de datos: template ---> db.php y 
template ---> coneccion.php.(Aqui se configuran parametros de 
conexion, Ojo se necesitan los 2 archivos).

en index, cuando se llaman las funciones galeria y mostras imagenes
pasar los siguientes parametros 

galeria(FotosPorPagina ,IdDeEvento);
mostrarImagenes(IdDeEvento);
